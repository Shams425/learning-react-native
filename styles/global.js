import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  container: {
    display: "flex",
    flex: 1,
    width: 200,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});
